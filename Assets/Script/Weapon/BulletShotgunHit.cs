using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletShotgunHit : Bullet
{
    // Start is called before the first frame update
    void Start()
    {
        OnInit(15);
    }
}
